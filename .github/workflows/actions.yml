name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
      uses: actions/checkout@v2
      - name: npm install and run tests
        run: npm install
          npm run test
